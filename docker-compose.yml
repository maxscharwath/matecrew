services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: matecrew
      POSTGRES_PASSWORD: matecrew
      POSTGRES_DB: matecrew
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
